* commands
> gnuplot hoge v(x) v(y)
-->
hoge.plt  # gnuplotコマンド)
hoge.data # gnuplot描画用データ)
hoge.eps  # グラフ
が出力される

* 記法: エレメント
- アルファベットと数字
- 大文字小文字は区別されない
- 1文字目でエレメントの種類が決まる

  | R | 抵抗            |
  | C | コンデンサ      |
  | M | MOSトランジスタ |
  | V | 電圧源          |

** コンデンサ
c0    in   0    12f


* 記法: ノード
- アルファベットと数字
- 「0」という名前のノードが回路中に必ず必要
  電圧の基準になる
