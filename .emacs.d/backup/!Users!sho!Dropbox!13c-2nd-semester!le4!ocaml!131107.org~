* ボーナス問題
- 4.2, 4.3 実行例??
- テストケースを全て通してバグがあるのを発見したらボーナス点
- Ex.4.5.2


* 4.2
- pp_ty, rec にしてしまった…

** 旧：型推論アルゴリズム (ty_decl tyenv exp)
入力：型環境Γと式e
出力：Γ⊢e:τ という型判断が導出できるような型τ

** 新：型推論アルゴリズム
入力：型環境Γと式e
出力：SΓ⊢e:Sτ という型判断が存在するような型代入Sと型τ

* まとめ
** 型環境 Γ
   変数と型のペアのリスト
   type tyenv = ty Environment.t
   environment ... ((Syntax.id, 'a) list) (連想リスト) で定義

** 型代入 S
   type subst = (tyvar * ty) list
   型変数(tyvar = int) と 型(ty) の連想リスト
   
** 型判断
   式eが型\tau を持つという判断
   \Gamma |- e:\tau
