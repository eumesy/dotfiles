#ifndef PHYLOGENETIC_TREE_HH
#define PHYLOGENETIC_TREE_HH

using namespace std;

struct tree_node {
  int node_number;
  bool has_child;
  tree_node* fst_child;
  tree_node* snd_child;
  vector<string> profile;
};

extern const int NUM_OF_SEQ_MAX;

// std::vector<tree_node> make_guide_tree(std::vector<std::string> &seq, double (&distance)[NUM_OF_SEQ_MAX][NUM_OF_SEQ_MAX]);
// void make_guide_tree(vector<string> &seq, vector<vector<double> >&distance, vector<tree_node> &guide_tree);
vector<tree_node> make_guide_tree(vector<string> &seq, vector<vector<double> > &distance);

#endif
