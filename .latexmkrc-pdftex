#!/usr/bin/env perl

$latex      = 'pdflatex -interaction=nonstopmode -halt-on-error -file-line-error';
$pdf_mode   = 1; # generate pdf via pdftex
$bibtex     = 'bibtex';
$bibtex_use = 2; # use .bbl files (bibtex or biber); delete .bbl when cleaning (-c, -C)
$max_repeat = 10;

$pdf_previewer               = "open -ga /Applications/Skim.app";
$pvc_view_file_via_temporary = 0;
